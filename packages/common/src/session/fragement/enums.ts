export enum AssemblyFragmentType {
  CODE = 'code',
  DOCUMENTS = 'text/markdown',
  HTML = 'text/html',
  SVG = 'image/svg+xml',
  MERMAID = 'mermaid',
}

export enum ExternalFragmentType {
  DOCUMENTS = 'markdown',
  HTML = 'html',
  SVG = 'svg',
  MERMAID = 'mermaid',
}
