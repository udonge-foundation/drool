export enum IndustryBinaryName {
  Drool = 'drool',
}

export enum IndustryBinaryS3Prefix {
  Drool = 'industry-cli',
}

export enum TodoDisplayMode {
  Inline = 'inline',
  Pinned = 'pinned',
}

export enum ToolResultDisplay {
  Expanded = 'expanded',
  Compact = 'compact',
}

export enum LogoAnimationMode {
  Once = 'once',
  Always = 'always',
  Off = 'off',
}

export enum DiffMode {
  Github = 'github',
  Unified = 'unified',
}

export enum SessionSurface {
  InteractiveTui = 'interactive-tui',
  Exec = 'exec',
  SubAgent = 'sub-agent',
  Daemon = 'daemon',
}
