// Fixed content height for the plugin menu (reskin only).
// Browse tab uses 4 rows of overhead: search (1) + margin (1) + pagination (1) + margin (1),
// so the total content area is PLUGIN_CONTENT_HEIGHT = overhead (4) + list rows (8).
export const PLUGIN_CONTENT_HEIGHT = 12;
