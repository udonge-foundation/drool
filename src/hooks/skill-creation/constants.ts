/**
 * Locale-independent marker used inside skill-creation session titles.
 * Detection code checks for this constant instead of a translated string prefix,
 * so it works regardless of the UI language.
 */
export const SKILL_CREATION_TITLE_MARKER = '[skill-creation]';
