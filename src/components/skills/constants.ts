// Fixed content height for the skills menu (reskin only).
// SkillListTab uses 4 rows of overhead: search (1) + margin (1) + pagination (1) + margin (1),
// so the total content area is SKILLS_CONTENT_HEIGHT = overhead (4) + list rows (8).
export const SKILLS_CONTENT_HEIGHT = 12;
